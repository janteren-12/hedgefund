__pycache__/
*.pyc
.venv/
venv/

# data/filings.db is intentionally NOT ignored - it's committed on
# purpose so the deployed site (which has no persistent disk on Render's
# free plan) ships with real data on every deploy. See
# .github/workflows/refresh.yml, which keeps it up to date.

.vercel
.env*
